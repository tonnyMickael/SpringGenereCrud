package com.dev.template_crud.service;

import java.util.List;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;
import org.springframework.data.domain.*;
import com.dev.template_crud.model.*;
import com.dev.template_crud.repository.[ClassName]Repository;

@Service
public class [ClassName]Service {
	
	@Autowired
	private [ClassName]Repository [varClassName]Repository;

	public Page<[ClassName]> listPaginated(Pageable pageable){
		return this.[varClassName]Repository.findAll(pageable);
	}
	
	public int count() {
		return (int)this.[varClassName]Repository.count();
	}

	[add]

	[fetch]

	[update]

	[delete]

	[detail]

} 
