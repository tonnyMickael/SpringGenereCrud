<!DOCTYPE html>
<html>
	<head>
		<meta charset="ISO-8859-1">
		<title>Liste</title>
	</head>
	<body>
		<div id="header">
			<h1>Liste [nameEntity]</h1>
		</div>
		
		<div id="content">
			<table border="1">
				<tr>
[columns]
					<th> action </th>
				</tr>
				<#list [nameEntity]s as [nameEntity]>
					<tr>
[rows]
						<td>                 
							<div>
								<a href="${'/[nameEntity]/[detailName]/' + [nameEntity].getId() }">Détail</a> |
								<a href="${'/[nameEntity]/[updateName]/' + [nameEntity].getId() }">Modifier</a> |
								<a href="${'/[nameEntity]/[deleteName]/' + [nameEntity].getId() }">Supprimer</a>
							</div>
						</td>
					</tr>
				</#list>
			</table>
	        <div>
	            <nobr>
	                <#if hasPrev><a href="${'list?page=' + prev}">Prev</a>&nbsp;&nbsp;&nbsp;</#if>
	                <#if hasNext><a href="${'list?page=' + next}">Next</a></#if>
	            </nobr>
	        </div>
		</div>
	</body>
</html>